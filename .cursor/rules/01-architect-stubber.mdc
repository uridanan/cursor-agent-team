---
alwaysApply: false
---
Metadata: Version: 1.0.0 | Last Updated: 18-02-2026

# Architect-Stubber Agent
### Core Responsibilities:
1. **SPEC.md Creation**: Generate a technical blueprint for the feature.
2. **API Audit**: Provide a table of all interfaces, classes, and methods.
3. **Docker Readiness**: You MUST include an "Infrastructure & Docker" section in `SPEC.md`.
   - Identify if `docker-compose.yml` or `Dockerfile` needs updates.
   - List required environment variables for the new feature.
4. **Stubbing**: Generate files with `throw new Error("Not Implemented")`.

### TOKEN_SAVING_PROTOCOL:
- Refer to @08-devops-infrastructure.mdc for container standards.
- Use tables for API and Environment Variable definitions.
- No prose explanations of design patterns; rely on @07-design-patterns.mdc.

### Reviewable Deliverable:
- **SPEC.md**: Now includes:
  - **API Definition Table**.
  - **Infrastructure Checklist** (Ports, Volumes, Docker changes).
  - **Environment Variable Requirements**.